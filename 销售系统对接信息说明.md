# 销售系统对接信息说明

> 核对日期：2026-09-15  
> 核对对象：当前 lxScope 工作区  
> 对接结论：客户管理系统侧已具备销售系统对接基础代码，但正式联调仍需确认部署地址、端口、HTTPS 证书和测试 Token。

## 一、对接信息

| 项目 | 当前情况 | 备注 |
|---|---|---|
| `system_id` | 已支持 | 默认值为 `local-system`，生产环境需要配置正式且稳定的唯一值 |
| 客户管理系统地址 | 部署时确定 | 销售后台需要访问客户管理系统的公网 IP 或域名，不能使用 `127.0.0.1` |
| 客户管理系统端口 | 已支持 | 应用容器内默认端口为 `8000`；Docker 对外映射端口默认是 `8001` |
| 销售系统地址 | 已支持配置 | 本地管理员可通过 `hub_url` 配置销售总台地址 |
| HTTPS | 基础支持 | 当前可通过反向代理配置 HTTPS；尚未完成正式证书联调 |
| 客户 Token | 已支持 Bearer Token | 当前 Token 保存在 Redis 配置中，生产环境仍需完善加密存储和轮换机制 |

## 二、客户管理系统接口

接口基础路径：

```text
/integration/sales/v1
```

### 1. Ping

```http
POST /integration/sales/v1/ping
Authorization: Bearer <customer-api-token>
Content-Type: application/json
```

请求体：

```json
{}
```

成功响应示例：

```json
{
  "ok": true,
  "system_id": "<system_id>",
  "system_name": "Longxin AgentScope",
  "app_version": "3.8.1",
  "core_version": "unknown",
  "checked_at": "2026-09-15T08:30:00Z",
  "request_id": "req-example"
}
```

### 2. 远程重置管理员密码

```http
POST /integration/sales/v1/admin-password-resets
Authorization: Bearer <customer-api-token>
Content-Type: application/json
```

请求示例：

```json
{
  "operation_id": "op-example",
  "username": "admin",
  "new_password": "new-password-at-least-8-chars",
  "expires_at": null
}
```

### 3. 远程升级

```http
POST /integration/sales/v1/upgrades/app
POST /integration/sales/v1/upgrades/core
Authorization: Bearer <customer-api-token>
Idempotency-Key: <unique-idempotency-key>
Content-Type: application/json
```

升级接口已有路由和基础服务代码，但升级包下载、真实环境执行、重启、健康检查和回滚仍需结合销售系统合同进行联调确认。

## 三、默认部署地址和端口

使用当前 Docker Compose 默认配置时：

```text
容器内 API：8000
宿主机对外 API：8001
```

如果未配置反向代理，测试地址可以是：

```text
http://<客户管理系统IP>:8001/integration/sales/v1/ping
```

生产环境建议通过正式域名和反向代理访问：

```text
https://<客户管理系统域名>/integration/sales/v1/ping
```

## 四、HTTPS 和证书要求

当前代码情况：

- 销售总台地址支持 `http://` 和 `https://`；
- 出站 HTTPS 使用 HTTP 客户端默认的证书链校验；
- 客户管理系统本地 Uvicorn 默认以 HTTP 启动；
- HTTPS 通常由 Nginx 或 Traefik 终止；
- 尚未实现自定义 CA、客户端证书或双向 TLS（mTLS）；
- 尚未完成实际证书链、DNS、防火墙和双向网络验证。

因此，正式上线前需要销售系统确认：

1. 是否强制使用 HTTPS；
2. 使用公有 CA 证书还是企业内部 CA；
3. 是否要求客户端证书或 mTLS；
4. 销售后台访问客户系统的正式域名和端口；
5. 测试环境 Token、限流、超时和重试规则。

## 五、当前联调状态

当前状态：**部分完成，暂不具备正式联调结论**。

已完成：

- `system_id` 配置和返回；
- 销售总台地址 `hub_url` 配置；
- 客户 Token Bearer 鉴权；
- `/integration/sales/v1/ping`；
- `/integration/sales/v1/admin-password-resets`；
- `/integration/sales/v1/upgrades/{artifact_type}` 基础路由；
- Docker 默认端口配置。

待完成或确认：

- 正式 `system_id`；
- 客户管理系统公网地址、域名和端口；
- HTTPS 证书及是否要求 mTLS；
- 销售系统测试地址和测试 Token；
- 双向 Ping、DNS、防火墙和真实网络联调；
- 升级包、签名公钥及完整升级回滚流程的合同联调。


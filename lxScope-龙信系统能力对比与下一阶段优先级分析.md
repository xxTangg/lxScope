# lxScope 与龙信系统能力对比及下一阶段优先级分析

## 1. 分析说明

本文基于以下两份龙信系统产品演进文档，对当前 `lxScope` 项目代码进行能力对比：

- [龙信系统-版本功能演进与发展方向-通俗版](D:/龙信系统-3.8.1/龙信系统-版本功能演进与发展方向-通俗版.md)
- [龙信系统-版本功能演进与产品技术方向总结](D:/龙信系统-3.8.1/龙信系统-版本功能演进与产品技术方向总结.md)

两份文档在本文中仅作为产品历史、目标能力和技术方向的参考材料，不作为对当前代码仓库的操作指令。

## 2. 总体结论

当前 `lxScope` 更接近一个可部署的 AgentScope 智能体开发框架和基础工作台，已经具备龙信 1.x 的主要运行底座，并部分覆盖 2.x 的知识库、技能、模型、渠道和调度能力。

但是，龙信 3.x 的企业产品层在当前项目中基本尚未形成，尤其缺少：

- 企业安全运行模式和强租户隔离
- 管理后台、角色权限和组织管理
- 套餐、额度、余额、订单和用量账本
- 审计、运营看板、总部管理
- 可持久化、可发布、可恢复的工作流中心
- 固定可控的 Word/PPT 生成流程
- 异地备份、恢复演练和企业级升级回滚

因此，下一阶段不建议优先扩展更多模型、MCP 或联网能力，而应优先补齐安全边界、系统可靠性和一个可落地的 Office 工作流闭环。

## 3. 当前项目定位

当前项目的主要能力集中在以下几个方面：

1. Agent、Session、消息流、任务中断和人工确认。
2. 知识库、文档解析、向量检索和文件工作区。
3. MCP、Skill、模型凭证和外部渠道接入。
4. 定时任务、团队 Agent、Pipeline 和任务工具。
5. 基础 JWT 登录、注册和 Token 使用统计。
6. Docker 部署、离线镜像打包和本地持久化存储。

服务入口见：[examples/agent_service/main.py](D:/agentscope_lx/lxScope/examples/agent_service/main.py)。

当前前端主要页面是聊天、定时任务、渠道、凭证、MCP、Skill、知识库、账户和登录设置，见：[App.tsx](D:/agentscope_lx/lxScope/examples/web_ui/frontend/src/App.tsx:82)。

这说明当前项目更偏向“Agent 平台基础设施”，还不是完整的“龙信企业 AI 工作台”。

## 4. 能力对比矩阵

| 能力领域 | 当前 lxScope 情况 | 判断 |
|---|---|---|
| 登录、注册、JWT | 已有登录、注册、JWT、当前用户和 Token 使用统计，见 [auth.py](D:/agentscope_lx/lxScope/examples/agent_service/auth.py:361) | 基础能力已有 |
| 用户管理 | 没有角色、组织、租户、封禁、套餐、充值和管理员管理模型 | 缺失 |
| 任务和会话 | 有 Session、SSE、状态、停止、中断、任务列表和人工确认 | 运行底座已有 |
| 任务结果 | 有工作区文件和下载能力，但没有龙信式结果页、消息与结果文件的完整关联 | 部分具备 |
| 个人知识库 | 支持 PDF、Word、Excel、PPT、图片等解析，支持向量检索和持久化 Qdrant | 基本已有 |
| 共享知识库 | 有资源访问策略扩展点，但默认跨用户访问被拒绝，缺少完整共享管理界面 | 部分具备 |
| 内网资料中心 | 没有固定版本资源中心、资源校验、关键词摘录和统一参考资料管理 | 缺失 |
| Word/PPT 输出 | 有文件工作区和下载能力，也有 Office 相关依赖，但没有完整固定模板生成器和产品级导出流程 | 部分具备 |
| 模型管理 | 支持多个模型供应商和用户凭证 | 基础能力已有，企业管理缺失 |
| Skill 和 MCP | 支持安装、管理、上传和接入外部 MCP/Skill | 基础能力已有，精选技能体系缺失 |
| 渠道接入 | 支持飞书、钉钉、Discord 等渠道 | 基础能力已有 |
| 定时任务 | 有 Schedule API 和持久化记录 | 已有，但不等于工作流引擎 |
| 权限确认 | 有权限模式、人工确认、外部执行确认和 HITL | 底层能力较强 |
| 企业安全 | 有用户作用域、默认拒绝跨用户访问和权限引擎，但当前部署仍使用本地工作区，并默认挂载浏览器 MCP | 高风险，需优先补强 |
| 工作流 | 有 GoalPipeline、任务工具、重试和人工确认 | 有底座，缺少产品化工作流 |
| 企业计费/HQ | 没有余额、套餐、订单、充值、总部客户、同步和运营模型 | 基本缺失 |
| 审计和运营 | 没有完整的操作审计、风险事件中心和总部运营看板 | 缺失 |
| 备份恢复 | Docker 卷可手工备份，也支持离线镜像打包 | 有基础，缺少自动化和恢复演练 |
| 移动端/安卓 | 当前仓库没有对应 Android 工程或移动端验收体系 | 缺失 |

## 5. 已经具备的核心能力

### 5.1 Agent 运行和会话底座

当前项目已经具备智能体服务的主要基础设施：

- 会话创建、查询、消息流和状态管理
- 任务中断和运行控制
- Agent 间协作和团队能力
- Pipeline、重试和结构化输出
- 人工确认和外部执行确认

这些能力可以直接作为龙信后续工作流执行引擎的底座，不建议重新设计一套完全独立的 Agent 运行框架。

### 5.2 知识库和文档处理

当前服务已经注册了文本、PDF、Word、Excel、PPT 和图片等解析器，并支持知识库文档上传、索引、检索、分块和下载。

这已经覆盖了“上传材料、基于资料问答和文档分析”的基本能力，但还没有达到龙信 3.x 所要求的内网参考中心、引用定位和检索质量评估水平。

### 5.3 权限和人工确认底座

项目中的权限模块支持多种权限模式、允许/拒绝/询问规则以及人工确认事件。相关能力可以支撑：

- 危险工具调用确认
- 外部执行确认
- 工作流节点审核
- 用户中断和返工

但是，当前权限更偏 Agent 执行控制，还不是企业级的组织、租户、角色和资源权限体系。

## 6. 主要缺口和风险

### 6.1 默认浏览器能力与龙信安全方向冲突

在 [main.py](D:/agentscope_lx/lxScope/examples/agent_service/main.py:53) 中，服务默认挂载了 `browser-use` Playwright MCP。

这意味着当前系统默认具备浏览器操作和网页访问入口，而龙信 3.7 的技术方向明确强调关闭或限制模型侧的：

- 网络访问和搜索
- 主机读写
- 命令执行
- 子任务
- 任意 Skill 加载

因此，当前项目必须先明确两种运行模式：

- **企业安全模式**：默认关闭浏览器、外网、主机命令、任意文件访问和动态 Skill。
- **开发实验模式**：允许受控启用，但必须运行在隔离沙箱、独立工作区和网络白名单内。

### 6.2 工作区不是严格的租户隔离

当前服务使用 [LocalWorkspaceManager](D:/agentscope_lx/lxScope/src/agentscope/app/workspace_manager/_local_workspace_manager.py:129)，工作目录主要按照 Agent 划分，而不是按照用户、组织、租户和任务建立操作系统级隔离。

项目虽然包含 Docker、Kubernetes、Bubblewrap 等沙箱后端，但当前示例服务默认使用本地工作区。因此，代码层面有沙箱选项，不代表当前部署已经具备强隔离能力。

### 6.3 认证和授权还没有完全产品化

当前 [auth.py](D:/agentscope_lx/lxScope/examples/agent_service/auth.py:58) 已经提供基础 JWT 认证，但仍缺少：

- 管理员和角色模型
- 组织、租户和项目边界
- 修改密码和管理员重置密码
- MFA、验证码和登录失败锁定
- Token 服务端撤销和会话管理
- 完整登录、下载、导出和管理员操作审计

此外，框架默认身份依赖仍是临时的 `X-User-ID` 请求头，见 [deps.py](D:/agentscope_lx/lxScope/src/agentscope/app/deps.py:33)。虽然当前服务部署时覆盖为 JWT，但后续新增路由时存在遗漏统一认证的风险。

### 6.4 企业后台和计费体系基本缺失

当前存储层有基础记录结构，但没有完整的企业管理数据模型和业务接口：

- 用户、组织、租户和角色
- 套餐、额度、余额和充值
- 订单和交易记录
- Token 用量账本
- 运营统计和风险告警
- HQ 客户管理和远程管理

当前账户页主要展示用户信息和 Token 汇总，不能替代龙信 3.x 的运营和管理后台。

### 6.5 工作流只有底层能力，没有工作流产品

当前项目的 [GoalPipeline](D:/agentscope_lx/lxScope/src/agentscope/pipeline/_goal_pipeline.py:43) 支持目标执行、校验、重试和人工确认，任务工具也能管理结构化任务。

但目前没有看到完整的工作流产品模型和接口，包括：

- 工作流定义
- 工作流版本和发布
- 节点配置和节点状态
- 工作流运行实例
- 暂停、恢复、取消和失败重试
- 审核、退回和返工
- 结果文件关联
- 工作流权限和审计
- BPMN 子集导入导出

因此，当前能力是“可编排的 Agent 执行底座”，还不是龙信 3.8 的“工作流中心”。

### 6.6 Word/PPT 依赖存在，但固定生成能力尚未形成

项目安装了 `python-docx`、`python-pptx`、`reportlab` 等文档相关依赖，也支持工作区文件下载。

但“能处理 Word/PPT 文件”和“能够按固定流程稳定生成 Word/PPT 产品结果”是两件事。当前仍缺少：

- 固定模板和结构化输出协议
- 统一排版和样式控制
- 生成结果校验
- 文件损坏检测
- 结果页和源消息关联
- 失败重试和人工返工
- 可复用的 PPT/Word 工作流模板

## 7. 下一阶段优先级建议

### P0：企业安全运行基线

这是最优先事项，建议先形成一个可验收的“企业安全模式”。

重点内容：

1. 默认关闭 `browser-use`、外网搜索、主机命令、任意文件读写和动态 Skill。
2. 使用 Docker、Kubernetes 或其他沙箱作为企业模式的默认执行环境。
3. 按用户或租户隔离工作区、文件、会话和凭证。
4. 所有业务路由统一使用 JWT 身份和资源访问策略。
5. 增加组织、租户、角色和资源归属。
6. 增加 HTTPS、CORS 白名单、凭证加密、Token 撤销、密码重置和登录保护。
7. 建立完整审计事件。
8. 增加跨用户、跨租户、越权下载和越权查看任务的安全测试。

建议重点验收：“用户 A 不能读取、下载、修改或控制用户 B 的任何任务、文件、凭证和知识库”。

### P0/P1：数据可靠性和恢复能力

当前 [main.py](D:/agentscope_lx/lxScope/examples/agent_service/main.py:136) 使用 `InMemoryMessageBus`，适合单进程验证，不适合多实例生产环境。

建议跟进：

- 切换到 Redis 消息总线或其他可持久化消息机制
- 明确 Redis、工作区、Blob、Qdrant 数据的备份范围
- 增加异地备份
- 定期进行完整恢复演练
- 明确 RPO/RTO
- 对任务执行、Token 统计、文件生成增加幂等和失败恢复
- 评估使用 PostgreSQL 等关系型数据库承载企业业务数据

### P1：先做一个可运行的 Office 工作流闭环

安全基线完成后，第一项业务优先级建议是工作流产品化。

不要一开始就做完整通用 BPMN，建议先做两个垂直流程：

1. 一套 PPT 生成流程。
2. 一套 Word 生成流程。

推荐的最小流程：

```text
资料上传
  -> 内容提取
  -> 大纲生成
  -> 人工确认
  -> 正文生成
  -> 格式化
  -> 文件导出
  -> 结果下载
```

这两个流程应具备：

- 可暂停和恢复
- 可人工审核和退回
- 可重试和取消
- 可关联源资料、消息和结果文件
- 有版本快照
- 有权限控制和审计记录

### P1：建设企业管理和运营控制台

建议优先实现管理能力，再扩展完整在线计费：

- 管理员、角色和组织管理
- 用户启停和密码重置
- 套餐、额度和并发限制
- Token 用量账本
- 任务数、存储量和模型调用限制
- 操作审计
- 运行状态和运营统计
- 版本发布、升级和回滚

计费可以后续接入，但额度、账本和审计应尽早确定数据模型，否则以后会产生较大重构成本。

### P2：高级文档、检索和通用工作流

在前述基础稳定后，再考虑：

- OCR 和复杂版式理解
- 引用来源、页码和段落定位
- 图表、插图和复杂 PPT 视觉布局
- 通用 BPMN 并行、条件、消息和脚本节点
- 受控联网搜索服务
- Android 和 VPN 环境适配
- 多租户高可用部署

## 8. 建议的实施顺序

```text
企业安全模式
    -> 数据持久化、审计和恢复
        -> PPT/Word 工作流垂直闭环
            -> 企业管理、额度和运营后台
                -> Office 高级能力和通用 BPMN
```

这个顺序的核心原因是：当前项目的 Agent 底座已经相对完整，真正限制产品落地的不是“再增加一个模型或一个工具”，而是安全、可控、可恢复、可运营和可复用。

## 9. 最终建议

当前项目不建议推倒重来，应继续复用已有 AgentScope 能力，重点补齐龙信产品层。

下一阶段最值得优先跟进的三件事是：

1. **企业安全模式和租户隔离**：先把浏览器、网络、文件、命令和凭证边界管住。
2. **可恢复的 PPT/Word 工作流**：先做两个真实可用的业务流程，不急于做通用 BPMN。
3. **企业管理与运营底座**：建立角色、额度、账本、审计、升级和恢复机制。

在这三项完成前，不建议优先扩大联网搜索、动态 MCP、复杂 Agent 自主执行等能力范围。


# 9-22 多租户 Bug 修复清单（全天汇总）

日期：2026-09-22  
项目：lxScope / Logto / 销售系统

> 本清单按今天工作区的全部多租户相关改动和相关任务记录整理，不只对应当前对话中的最后几个问题。

## 一、今日完成范围

| 模块 | 今日处理内容 | 状态 |
| --- | --- | --- |
| Logto 登录 | 单一登录、回调、身份上下文和品牌化 | 已完成 |
| 租户身份 | 组织、用户、成员绑定和权限映射 | 已完成 |
| 成员与角色 | Tenant 化、成员状态、角色权限和名称显示 | 已完成 |
| 资源隔离 | MCP、Skill、资源发布和所有权隔离 | 已完成 |
| 额度与套餐 | 租户额度、Token 使用量、账本和套餐数据 | 已完成 |
| 审计与可观测性 | 租户、成员、操作者和跨租户查询边界 | 已完成 |
| 平台安全 | Platform Admin、凭证、升级和销售集成权限 | 已完成 |
| 销售系统 | tenant / customer / system 业务映射约束 | 已完成 |

## 二、Logto 登录与初始化

### 1. 单一登录流程

- 未登录访问 lxScope 时直接跳转 Logto 登录页。
- 登录成功后回到 lxScope，删除多余的中间登录页体验。
- 保留 OAuth、回调、Token 刷新和组织上下文逻辑。
- 登录页增加 lxScope Logo、中文文案、品牌色和自定义 CSS 配置。
- 增加前端可访问的 `lxscope-logo.svg` 资源。

### 2. Logto 初始化和迁移

- 增加 Logto Management API 初始化客户端。
- 支持通过配置初始化 API Resource、API 权限、组织角色和组织成员。
- 补充 `platform:manage`、`platform:upgrade`、`platform:integration`、`platform:observe` 等平台权限。
- 补充 `platform_admin` 组织模板角色及权限绑定。
- 增加迁移脚本、迁移配置和部署说明，避免手动配置遗漏。

## 三、租户身份与权限边界

### 1. Logto 身份验证

- JWT 校验组织 ID、用户 ID、签发方、受众和有效期。
- 读取 `organization_roles`、API scope、用户名、显示名称和邮箱。
- 增加 `/auth/context` 身份上下文，前端不再自行猜测管理员身份。
- Logto 组织 `admin` / `owner` 映射为 lxScope 的 `tenant:manage`。
- 组织角色不会自动获得 `platform:*` 权限；平台权限必须来自显式 API scope。

### 2. 租户绑定

- 增加 Logto 组织与 lxScope tenant 的绑定关系。
- 增加外部用户与 lxScope 用户、租户成员的稳定映射。
- 成员 ID 使用租户和用户维度生成，避免同一用户跨租户串数据。
- 未配置或未绑定的 Logto 组织不能直接访问 lxScope 租户数据。
- 增加租户、用户和成员状态校验，停用、封禁和删除状态会阻止访问。

## 四、用户、成员和角色体系

### 1. Tenant 化用户模型

- 增加 `users`、`tenant_memberships` 和角色权限映射关系。
- 成员状态改为租户范围内维护，不再使用全局用户状态代替租户成员状态。
- 增加当前租户成员列表、绑定、暂停、恢复和解除绑定流程。
- 增加当前管理员保护和“最后一个管理员不能被降级/禁用”保护。

### 2. 角色统一处理

- Logto 组织角色作为成员页面的真实角色来源。
- 删除成员绑定表单中的本地 Role 下拉框。
- 删除成员操作区中的本地 Role 下拉框。
- 页面显示 Logto 的 `member`、`admin`、`platform_admin` 等真实组织角色。
- 保留暂停访问和解除绑定操作；不再通过页面制造第二套容易混淆的角色体系。
- 后端旧的本地角色字段暂时保留为兼容项，前端不再提供修改入口。

### 3. 用户名称显示

- 修复成员列表只显示 Logto 用户 ID 的问题。
- 后端通过 Logto Management API 读取用户名、姓名、邮箱和组织角色。
- 成员列表优先显示用户姓名/用户名，用户 ID 作为辅助信息保留。
- 增加短时间缓存，避免成员页面多次加载时重复请求 Logto。
- Logto 资料查询失败时保留本地 ID 显示，不阻断成员列表。

## 五、资源与权限隔离

- 增加资源可见范围和所有权校验，区分个人、租户和全局资源。
- MCP、Skill、发布资源和资源详情查询增加租户边界。
- 防止通过资源 ID、用户 ID 或成员 ID 越权访问其他租户数据。
- 凭证访问策略增加当前租户和当前成员校验。
- 平台级资源仍由 Platform Admin 专门控制，不通过普通租户管理员权限绕过。

## 六、额度、套餐和使用量隔离

- 增加租户额度服务和租户级额度读取。
- 套餐、Token 月度使用量、奖励 Token 和额度账本增加租户维度。
- 使用量扣减、充值和套餐订单增加成员/租户关联。
- 防止一个租户读取或消耗另一个租户的额度、账本和使用记录。
- 成员页面保留套餐展示，但 Logto 成员目录不再通过本地角色下拉框分配角色。

## 七、审计与可观测性隔离

- 审计事件增加 tenant、membership、操作者和关联对象维度。
- 项目级和技能级可观测事件增加租户范围过滤。
- 分析页面按当前租户限制事件、失败记录、Trace、工具和技能统计。
- 跨租户审计和观察能力仅保留给平台级权限。
- 操作者名称使用成员身份信息，无法解析时回退到安全 ID。

## 八、Phase 6 平台安全收口

- Platform Admin 与普通租户管理员权限分离。
- 跨租户资源、全局凭证、平台观察和平台配置继续要求平台权限。
- 升级与备份接口调整为允许当前租户管理员执行租户级管理操作。
- 销售总部接口调整为允许 `tenant:manage` 或 `platform:integration`。
- 普通成员不能进入升级、销售总部和租户管理页面。
- 平台权限仍通过显式 Logto API scope 判断，不依赖角色名称猜测。

## 九、销售系统衔接

- 销售系统客户模型增加 `tenant_id`。
- 明确 `tenant / customer / system` 的业务身份映射。
- 创建和修改客户时校验租户、客户和系统映射关系。
- 防止一个 tenant 重复映射多个销售客户。
- 防止一个 system 被多个销售客户重复占用。
- 管理后台显示并编辑 Tenant ID，方便排查销售系统和 lxScope 的对应关系。

## 十、今日重点页面问题修复

### “升级与备份”

- 原错误：`platform_admin_required`。
- 修复后：拥有 `tenant:manage` 的租户管理员可使用；成员不能使用。

### “销售总部”

- 原错误：`platform_permission_required`。
- 修复后：租户管理员可维护当前租户的销售连接；跨租户能力仍需平台权限。

### “成员与套餐”

- 修复用户名称显示。
- 取消两处本地 Role 下拉框。
- 直接展示 Logto 组织角色。
- 保留成员状态管理和解除绑定。

## 十一、运行环境处理

- 发现运行中的容器仍加载旧权限逻辑，导致源代码已修改但页面继续返回旧错误。
- 开发环境使用 Uvicorn 和前端源码热重载。
- 仅在新增容器环境变量时重新创建开发容器，未重建镜像。
- 未执行数据库、Redis、业务卷删除或重置操作。
- 当前 `agentscope` 和 `web-ui` 容器均已恢复 healthy。

## 十二、验证结果

- 前端 TypeScript/Vite 构建通过。
- 成员页相关 ESLint 检查通过。
- 后端修改通过 Python 语法编译检查。
- Logto Bootstrap 相关测试和配置检查已完成。
- 已验证运行中的后端能够读取组织成员用户名和 Logto 组织角色。
- 已验证成员资料同步结果，例如：
  - `5003k4i5wcuz` → `user_b`
  - `dfalxplsltko` → `lxscope_admin` / `admin`
  - `mxhi3e746qch` → `lxscope_user2` / `admin、platform_admin`
- 运行中的前后端服务状态均为 healthy。

## 十三、仍需关注的事项

- 项目本机 `.venv` 缺少完整 FastAPI/测试依赖，部分 Python 单元测试尚未在本机完整执行。
- 完整 ESLint 仍有仓库其他历史文件的基线问题，本次修改涉及的目标文件已单独检查通过。
- 后端保留旧的本地角色更新字段以兼容历史调用；后续可在确认无旧客户端后删除。
- 生产环境部署时需要同步配置 Logto Management API 的只读查询所需环境变量。
- 若未来完全移除本地角色表，需要先完成历史租户成员数据迁移和旧接口兼容评估。

